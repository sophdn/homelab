# Reserved per the T6 secrets convention. No secrets are currently extracted
# from toolkit-server/docker-compose.yml — the deployed instance is HTTP-only
# and does not consume secret-shaped env vars. If --default-project ever
# needs to vary by environment, this is the file it lives in.
